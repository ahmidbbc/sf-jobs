{% extends 'base.html.twig' %}

{% block title %}Skill{% endblock %}

{% block body %}
    <h1>Skill</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ skill.id }}</td>
            </tr>
            <tr>
                <th>SkillName</th>
                <td>{{ skill.skillName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('skill_index') }}" class="btn btn-primary">back to list</a>

    <a href="{{ path('skill_edit', {'id': skill.id}) }}" class="btn btn-light">edit</a>

    {{ include('skill/_delete_form.html.twig') }}
{% endblock %}
